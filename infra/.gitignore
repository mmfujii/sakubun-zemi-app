# CDK が実行のたびに生成する出力（ビルド成果物・コミット不要）
cdk.out/
.cdk.staging/

# コンテキストキャッシュ（AZ参照結果＋アカウントIDが入る）。
# 慣習上はコミットしてもよいが、公開リポジトリにアカウントIDを載せないため除外。
# AWS認証があれば synth 時に自動で再取得される。
cdk.context.json

# TypeScript ビルド出力
dist/
