# docker compose の web ビルド用（変数展開に使われる）。
# 実体の .env を作り、apps/web/.env.local から同じ値をコピーしてください。
# これらは NEXT_PUBLIC_*（＝ブラウザに焼き込まれる公開値）なので秘密ではありません。
# 実体の .env は .gitignore 済み。

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
