services:
  db:
    image: postgres:16-alpine
    container_name: sakubunzemi-db
    environment:
      POSTGRES_USER: sakubun
      POSTGRES_PASSWORD: sakubun
      POSTGRES_DB: sakubunzemi
    ports:
      - "5433:5432"
    volumes:
      - sakubunzemi_db_data:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  sakubunzemi_db_data:
